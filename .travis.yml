language: bash
before_script:
  - sudo apt-get install ia32-libs
  - npm install
  - npm install -g grunt-cli
  - chmod -R 0777 ./bin && ./bin/amxx-install.sh
script:
  - cd build/packages/base/addons/amxmodx/scripting && ./amxxpc -v
